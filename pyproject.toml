[project]
name = "abn-capstone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lucy Sheppard", email = "Lucy.Sheppard@xebia.com" }
]
requires-python = ">=3.9"
dependencies = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocs-material-extensions>=1.3",
]

[project.scripts]
abn-capstone = "abn_capstone:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
